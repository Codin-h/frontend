

export enum Rotations {
    ZERO = 0,
    HALF_PI = Math.PI / 2,
    PI = Math.PI,
    NEGATIVE_HALF_PI = -Math.PI / 2
}

export enum Parts { BUILDINGS, ROADS, CORNERS, JUNCTION, TSPLIT}

export enum ModelTypes {
    BUILDING_A,
    BUILDING_B,
    BUILDING_C,
    BUILDING_D,
    BUILDING_E,
    BUILDING_F,
    BUILDING_G,
    BUILDING_H,
    ROAD_A,
    ROAD_B,
    CORNER_A,
    CORNER_B,
    JUNCTION,
    INTERSECTION
}


